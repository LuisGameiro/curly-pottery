# TODO

- [ ] Fix ESLint `react/no-unescaped-entities` in `components/common/NewsletterBanner/NewsletterBanner.tsx` by escaping the apostrophe in `I'll`.
- [ ] Re-run `yarn lint` to confirm the error is resolved.

